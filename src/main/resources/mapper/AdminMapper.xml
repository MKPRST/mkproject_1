<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.admin.AdminMapper">

	<select id="getOrderList" resultType="com.example.demo.admin.AdminDto">
		select * from goodsOrder
		order by orderDay desc
	</select>

	<delete id="cancelOrder">
		delete from goodsOrder 
		where id=#{id} and userid=#{userid} and gcode=#{gcode}
	</delete>

	<select id="customerService" resultType="HashMap">
		select * from qna order by id desc
	</select>


	<update id="adminQnaAnswer">
		update qna set answer=#{answer} where id=#{id}
	</update>


</mapper>